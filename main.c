#include <SDL2/SDL.h>
#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int main(void)
{
    printf("DungeonSolverGame2D");
    int running = 1;
    while (running) {
        running = 0;
    }
    return 0;
}